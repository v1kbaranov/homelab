apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: open-webui
  namespace: homelab
spec:
  releaseName: open-webui
  interval: 15m
  values:
    pipelines:
      enabled: false
    persistence:
      enabled: false
      size: 1Gi
      storageClass: "local"
